version: "2.0.0"
blend:
  alpha: 0.5
  beta: 0.5
discovery:
  metric: l2
cache:
  artifact: alchemy_cache.json
framing:
  disclaimer: Alchemy merge is a 50/50 stat-vector blend in R^18 (11 core + showman + 6 shot zones) mapped to the nearest allowlist player via L2. Exploratory — not GOAT rank.
