# Deprecated path

Processed artifacts for this project live on the **data worktree**:

```text
GoatProject-data/processed/
```

This directory on `main` is a stale snapshot and is **not** used by the pipeline (`config/paths.yaml` -> `GoatProject-data/processed`).

After clone, run:

```bash
./bootstrap.sh
```
